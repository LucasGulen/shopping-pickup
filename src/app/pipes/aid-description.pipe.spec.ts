import { AidDescriptionPipe } from './aid-description.pipe';

describe('AidDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new AidDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
